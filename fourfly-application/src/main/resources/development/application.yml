# spring configuration
spring:
  profiles:
    active: developmen
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/fourflyoa?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
server:
  port: 8080

# LOGGING
# todo 注意logging
#logging:
#  config: classpath:logging-config.xml
